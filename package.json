{
  "name": "qdd-website",
  "version": "1.0.0",
  "private": true,
  "description": "钱多多个人主页 - 零依赖纯 HTML 站",
  "scripts": {
    "dev": "npx --yes serve -l 8000 -s .",
    "dev:py": "python3 -m http.server 8000",
    "dev:live": "npx --yes live-server --port=8000 --no-browser"
  }
}
